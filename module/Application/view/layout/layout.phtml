<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 */
?>
<?= $this->doctype() ?>
<html lang="en">

<head>
    <meta charset="utf-8">
    <?= $this->headTitle('Apoteka')->setSeparator(' - ')->setAutoEscape(false) ?>

    <?= $this->headMeta()
        ->appendName('viewport', 'width=device-width, initial-scale=1.0')
        ->appendHttpEquiv('X-UA-Compatible', 'IE=edge')
        ?>

    <!-- Styles -->
    <?= $this->headLink([
        'rel' => 'shortcut icon',
        'type' => 'image/vnd.microsoft.icon',
        'href' => $this->basePath() . '/img/favicon.ico'
    ])
        ->prependStylesheet($this->basePath('css/style.css'))
        ->prependStylesheet($this->basePath('css/bootstrap.min.css')) ?>

    <!-- Scripts -->
    <?= $this->headScript() ?>
</head>

<body>
    <nav class="navbar navbar-expand-md navbar-dark mb-4 " role="navigation">
        <div class="container ">
            <a class="navbar-brand" href="<?= $this->url('home') ?>">
                <img height="75px" src="<?= $this->basePath('img/apoteka-logo.png') ?>" alt="Apoteka">
                <span class="navbar-text text-light">Votre officine en ligne</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <?php
                $identity = $this->identity();
                /** @var Laminas\View\Helper\Url $urlHelper */
                $urlHelper = $this->plugin('url');
                $routeMatch = method_exists($urlHelper, 'getRouteMatch')
                    ? $urlHelper->getRouteMatch()
                    : null;
                $currentRoute = $routeMatch ? $routeMatch->getMatchedRouteName() : null;
                ?>
                <ul class="navbar-nav ms-auto align-items-center gap-2">
                    <li class="nav-item">
                        <a class="nav-link<?= $currentRoute === 'home' ? ' active' : '' ?>"
                           aria-current="page" href="<?= $this->url('home') ?>">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link<?= $currentRoute === 'product' ? ' active' : '' ?>" href="<?= $this->url('product') ?>">Produits</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link<?= $currentRoute === 'about' ? ' active' : '' ?>" href="<?= $this->url('about') ?>">À propos</a>
                    </li>
                    <?php if ($identity && $identity->hasRole('admin')): ?>
                        <li class="nav-item">
                            <a class="nav-link" href="<?= $this->url('user') ?>">Utilisateurs</a>
                        </li>
                    <?php endif; ?>
                </ul>

                <div class="d-flex align-items-center gap-3">
                    <?php if ($identity): ?>
                        <span class="text-white-50 small">
                            <?= $this->escapeHtml($identity->getEmail()) ?>
                        </span>
                        <a class="btn btn-outline-light btn-sm"
                           href="<?= $this->url('auth', ['action' => 'logout']) ?>">Déconnexion</a>
                    <?php else: ?>
                        <a class="btn btn-outline-light btn-sm"
                           href="<?= $this->url('auth', ['action' => 'login']) ?>">Connexion</a>
                        <a class="btn btn-warning btn-sm"
                           href="<?= $this->url('auth', ['action' => 'register']) ?>">Créer un compte</a>
                    <?php endif; ?>
                </div>


            </div>
        </div>
    </nav>
    <div class="container">
        <?= $this->content ?>
        <hr>
        <footer>
            <p>
                &copy; <?= date('Y') ?>
                <a href="https://irodev.nathan-demory.com">Nathan Demory</a> Using Laminas Framework.
            </p>
        </footer>
    </div>
    <?= $this->inlineScript()
        ->prependFile($this->basePath('js/bootstrap.min.js')) ?>
</body>

</html>